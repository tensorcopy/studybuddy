[project]
name = "studybuddy"
version = "0.1.0"
description = "Dependencies for running blog post code examples"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "scikit-learn>=1.3",
]
